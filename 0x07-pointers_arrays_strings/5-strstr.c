#include "main.h"
#include <stdio.h>

{
			if (haystack[i] != needle[i])
				break;

		}
		if (i == s)
			return (haystack);
		else
		if (i != s)
			haystack++;
		else
			return (haystack);
	}
	return (NULL);
}